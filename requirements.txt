# Pinned for reproducible builds. Versions chosen to clear the Trivy security
# gate: starlette 1.0.0 fixes CVE-2025-62727 (the <0.49.1 DoS); FastAPI 0.136.1
# requires only starlette>=0.46.0 so it's compatible.
fastapi==0.136.1
uvicorn[standard]==0.47.0
starlette==1.0.0
