# Keep the build context small and avoid leaking local/dev files into the image
.venv/
venv/
__pycache__/
*.py[cod]

.git/
.gitignore

# Terraform / k8s / CI artifacts don't belong in the app image
.terraform/
*.tfstate
*.tfstate.*
*.tfvars

.vscode/
.idea/
.DS_Store
Thumbs.db
README.md
